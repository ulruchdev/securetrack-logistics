databaseChangeLog:
  # =====================================================================
  # CBS Logistics - Tracking Service
  #
  # La table tracking_history est gérée par Liquibase.
  # Les tables Axon (domain_event_entry, snapshot_event_entry,
  # token_entry) sont créées par Hibernate via ddl-auto: update.
  # =====================================================================
  - include:
      file: 001-create-tracking-history.yaml
      relativeToChangelogFile: true
